language: python
python:
  - "2.7"

before_install:
  - "travis_wait 30 mvn install" 
  -"pip install -r requeriments.txt"

script:
  - "coverage run --source=wilenco/web manage.py test"
  - "pip install -r requirements.txt"

after_script: "coveralls"
